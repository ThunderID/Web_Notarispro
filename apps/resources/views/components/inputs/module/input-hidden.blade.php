<?php
	// init component

	// UI
	//Nothing to init
?>

	<input 
		id="{{ 'thunder_input_string_' . $data['name'] . $index }}" 
		name="{{ $data['name'] }}" 
		value="{{ $data['value'] }}" 
		class="thunder_input" 
		type="hidden" 
	>
